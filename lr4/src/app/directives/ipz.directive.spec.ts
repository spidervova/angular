import { IpzDirective } from './ipz.directive';

describe('IpzDirective', () => {
  it('should create an instance', () => {
    const directive = new IpzDirective();
    expect(directive).toBeTruthy();
  });
});
